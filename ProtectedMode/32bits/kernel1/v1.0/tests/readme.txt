These are test routines.
To run a test, use the "%include" directive to include a "test.asm" file in "kernel.asm". Then add a "jmp _teststart" instruction right after "_sysmain" label in "kernel.asm".
Only one test file may be included at a time.

